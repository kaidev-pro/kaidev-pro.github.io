/* eslint-disable @next/next/no-html-link-for-pages */
export async function generateStaticParams(){return ["8agents","8router","nihongogate","rakusaku","kai-revengers"].map(slug=>({slug}))} export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main className="section"><p className="eyebrow">PROJECT DETAIL</p><h1>{slug.replaceAll("-"," ")}</h1><p>Project hero, summary, status, role, process, features, technical approach, lessons, limitations, and next steps.</p><a className="secondary" href="/work/">Back to Work</a></main>}